/**
 * Automatically generated file
 *
 * {{=it.component.name}}
 * Created on: {{=new Date()}}
 */

{{=it.component.type}} {{=it.component.name}} { {{? it.uses_interface_refs && it.uses_interface_refs.length > 0}}
  uses { {{~ it.uses_interface_refs :iref}} 
    interface {{=iref.base }}{{? iref.type_arguments}}<{{=iref.type_arguments}}>{{?}}{{? iref.name }} as {{=iref.name}}{{?}}; {{~}}
  }{{?}}{{? it.provides_interface_refs && it.provides_interface_refs.length > 0}}
    provides { {{~ it.provides_interface_refs :iref}} 
      interface {{=iref.base }}{{? iref.name }} as {{=iref.name}}{{?}}; {{~}}
    } {{?}}
}
implementation {

  // Component
  {{~ it.component_refs :cref}} components {{=cref.base }}{{? cref.name }} as {{=cref.name}}{{?}};
  {{~}}
  {{~ it.generic_component_refs :cref}} components new {{=cref.base }}(){{? cref.name }} as {{=cref.name}}{{?}};
  {{~}}

  // Wiring {{~ it.link_wirings :wire}} 
  {{=wire.src }} -> {{=wire.dst}}; {{~}}

  // Equate {{~ it.equate_wirings :wire}} 
  {{=wire.src }} = {{=wire.dst}}; {{~}}

  // Module-implementation {{~ it.module_implementation :f}} 
  {{=f.type }} {{=f.name}} () {{{~ f.calls :c}}
    call {{=c.name}} ({{=c.ael}}); {{~}}
  }
  {{~}}

}
